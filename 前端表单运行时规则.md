# 前端表单运行时规则

## 1. 适用范围与规则归属

本文件是查询、新增、编辑、详情、弹窗、抽屉、设置、子表单、JsonSchema 和自定义业务表单运行时行为的正式来源。页面开发基准见《前端页面开发规则》，查询差异见《前端查询表单规则》，约束来源见《前端页面校验规则》，上传契约见《前端页面上传规则》，动作来源见《前端页面操作规则》。其他文件只引用共同契约，不重复维护。

“必须/不得”表示强制边界；“默认”表示未有明确配置时执行的基准；开发参考数字仍是 AI 应优先执行的基准，偏离须有局部理由和实际视觉验证，不能任意忽略。本规范描述目标契约，不表示当前所有组件已经完成实现或验收。

## 2. UI 设置优先与执行顺序

1. 先确定接口字段范围、权限、可写性、校验及必要技术参数。
2. 应用用户 UI 设置及全部权限、场景、业务显隐条件，分别确定有效业务字段集合、当前展示字段集合及明确保留的隐藏参数。
3. 基于有效业务字段集合及接口契约应用分组提交选择，确定本次业务校验和提交范围；不能直接以当前展示字段集合代替提交集合。
4. 基于当前展示字段集合及提交选择执行空组隐藏、单组简化、快捷填写、响应式布局及溢出处理。
5. 执行相应交互、异步加载、校验定位、保存及关闭。

UI 设置对其控制的属性优先于静态页面默认值、公共组件默认值和 AI 布局建议，但不能绕过接口或权限约束，也不是最后一道展示处理。后续优化不得恢复已排除字段、重写用户配置或扩大提交范围。

现有页面展示设置控制字段标题、隐藏、角色可见性、条件依赖/互斥、默认值、顺序、组归属和部分输入展示形式；控制组样式、顺序、初始展开和行数、提交勾选入口；控制查询自动查询/展开行数、弹窗最大宽高、详情空值、编辑自动强制更新。外围留白、背景、圆角、边框遵循用户界面设置。不得声称不存在于配置契约中的属性已由 UI 设置管理。

运行时按依赖增量重算，不是在每次输入时重置整个表单：仅尺寸/外观设置或容器变化时重排及检测溢出；其他设置按属性依赖重算，显隐及业务排除变化重新计算有效业务字段、展示字段、提交资格、空组和快捷资格；模式变化更新展示和焦点；校验失败展开错误区域。不得清空输入、重置提交选择或自动改写持久化配置。

## 3. 字段状态术语与校验提交资格

“看不见”不是统一的提交状态，开发配置和运行时必须明确区分：

| 状态 | 展示含义 | 本地校验与提交 |
| --- | --- | --- |
| 从本次有效表单排除 | UI 设置、权限或场景规则将字段移出本次有效业务字段集合 | 不校验、不提交、不加入强制更新；状态残留不例外 |
| 仅视觉隐藏或折叠 | 字段仍属于有效表单，只是不呈现输入区 | 按明确校验和提交契约处理，不因不可见自动省略 |
| 隐藏参数 | 明确声明需要传输而不供用户录入，例如主键、乐观锁 | 按接口契约携带、校验数据有效性，不强求可视输入 |
| 只读或禁用 | 限制用户编辑，不等于字段已从业务模型排除 | 以字段可写性和接口契约确定，不能统一按禁用决定 JSON 提交 |

此前“UI 隐藏后不提交”的业务要求，指第一行的排除语义，不是对所有 hidden 控件、CSS 隐藏或折叠的一刀切。实现须明确把设置解析成对应的有效状态，不能凭 `hidden` 名称自行扩大或改变语义。临时 `v-if` 卸载、虚拟渲染或 DOM 不存在，也不能替代业务字段排除。

UI 字段设置在查询、新增、编辑中使用横向连续紧凑分段按钮，选中高亮，不用圆点单选外观或下拉，按钮使用两字文案“展提/隐提/禁提/不提”，悬停 Tooltip 依次解释“展示控件并参与提交”“不展示控件仍参与提交”“展示控件但不可修改，仍参与提交”“不展示控件且不参与校验和提交”；语义如下：

| 选项 | 当前展示集合 | 参与字段集合 |
| --- | --- | --- |
| 展提 | 条件和权限允许时加入 | 按可写性及提交选择处理 |
| 隐提 | 不加入 | 初始化明确默认值并按契约校验、序列化、提交 |
| 禁提 | 加入但不可修改 | 保留默认值及原值，按权限、可写性和提交选择校验提交 |
| 不提 | 不加入 | 排除本地业务校验、请求及强制更新 |

“禁提”只限制用户交互，不等于接口字段不可写；它保留当前展示和按契约提交的资格，不计入快捷填写的可填写字段数。标准输入、选择、上传、弹出编辑、子操作和自定义字段入口必须执行同一禁用状态；控件变灰不能替代事件及值更新保护。禁提必填值缺失时，提示检查默认值或字段设置，不尝试聚焦不可编辑输入。

四项不能绕过权限、条件规则、接口可写性或用户取消组提交。隐藏提交不参与空组、快捷填写和可见布局计数，但默认值初始化不能依赖控件挂载；编辑原值按契约保留，不因默认值而任意覆盖。隐藏参与字段缺少必填值时给出表单级配置/值错误并阻止请求，不聚焦不存在控件。详情、列表只提供展示语义，不虚构提交动作。

原生 HTML 表单构造提交数据时，有 name、与表单关联且未禁用的 `input type="hidden"` 通常仍提交；CSS 隐藏不直接排除提交。禁用等条件另由 HTML 定义，HTTP 本身不判断控件可见性。当前项目以 JavaScript 组装 JSON，请求是否含某字段由载荷构造决定；必须显式过滤已排除字段，不能只删 DOM，也不能仅凭看不见而丢掉隐藏参数。参考 [HTML 表单条目构造标准](https://html.spec.whatwg.org/multipage/form-control-infrastructure.html#constructing-the-entry-list)。


- 经 UI 设置、权限、场景或业务规则从本次有效表单排除的业务字段，不校验、不提交、不加入 `forceUpdateFields`，即使本地存在旧值也不得补回。跨字段及类型转换校验必须尊重该范围，不能因已排除字段阻止本次保存；后端仍校验最终请求。
- 业务排除不得转换为提交空值。整个嵌套属性被排除时省略整个属性；部分子字段被排除时按实际接口嵌套更新契约组装，不擅自补 null 或补回已排除字段。
- 主键、乐观锁等必要技术参数按明确接口契约单独携带。上下文参数只有明确属于接口注入/定位契约时才单独提供，不能把已排除的租户、组织业务输入或旧值残留当作提交例外。
- 用户取消某组提交后，该组业务字段不校验、不提交、不强制更新。所有模式使用同一选择，不匹配选项的旧值、已修改值、默认值、转换器都不能覆盖取消选择。
- 分组折叠、“更多”收起、单组外壳简化只改变展示，不改变前述资格。
- 值保留、类型转换、主动清空、强制更新在确定提交范围之后执行，最终载荷及 `forceUpdateFields` 都须复核范围，防止转换器重新加入被排除字段。
- 选择控件在查询、新增未选择时默认省略；编辑主动清空且允许清空时提交空值及对应强制更新标记。参与提交的必填字段未满足校验时必须阻止保存，不得静默省略后提示成功。普通数字、布尔、文本不套用空选择规则，按类型契约序列化。
- 原值无法匹配选项时保留回显；仅在本次允许提交且用户未修改时按契约原样保存。加载失败不能自动清空、更换或因转换而丢弃原值。
- 单值保存、快捷开关、排序、状态变更及显式强制更新的局部请求设置 `autoForceUpdateField: false`，只携带必要标识和本次字段。完整编辑才按有效 UI 设置使用 `autoForceUpdateField`；无论开关如何，不能扩大字段资格。空值更新参数的 API 语义见《前端API开发规则》。

## 4. 分组提交、空组与单组

- 分组可配置可见角色和展示脚本。先完成角色和脚本条件，再判断空组、单组和折叠；角色不匹配、脚本返回 false 或执行失败时，整个分组及其字段从当前有效表单排除，不校验、不提交、不加入 `forceUpdateFields`。字段自身条件继续在组条件满足后执行。
- 分组展示脚本只使用当前表单、用户、组织和租户的明确上下文；不得扫描字段名猜测条件。查询分组不展示时，组内条件不进入查询参数；新增、编辑和详情分组不展示时，标题、边框、按钮、留白及字段一并消失。

- 普通组默认参与提交；`showSubmitCheckbox` 默认不展示，显示时每次打开默认勾选。复杂属性的独立提交机制不叠加普通组开关。
- 更新允许取消含必填字段的组；新增含有效必填字段或父属性必填约束的组不允许取消，全可选组可取消。
- 取消提交先确认内容本次不会保存、关闭后会丢失，取消确认保持原选择。确认后收起该组、保留本地输入和可编辑性，允许重新展开、修改和勾选；不即时保存或清空。
- 对接口允许省略且明确独立提交的可选复杂属性，原值 null 时默认不勾选、独立组默认收起；非 null 默认勾选。父必填约束按新增/更新及接口契约处理。未勾选提示“本次不变更这部分数据”。通用组不得提供清空已有对象能力；业务清空需独立权限、操作、确认及服务端契约。
- 完成全部显隐及详情空值过滤后，按折叠之前的有效展示字段判断空组。没有字段时隐藏标题、边框、按钮及留白并排除计数；恢复字段时恢复组，不删除配置。
- 新增编辑字段值为空不是空组；用户收起导致 DOM 不渲染也不是空组。普通模式取消提交后仍保留该组及恢复提交入口。
- 只剩一个有效业务组时，默认平铺字段，去掉无意义的组外壳；保留必要独立提交控制、父属性必填标识和对象列表本身。多个有效组保留区分。简化不删除映射或改变请求结构。
- 初次打开并完成回显后，优先执行 UI 设置明确的展开及行数策略；未明确配置时，超过四个有效组采用全可选组收起、有效必填组展开，不确定全可选则展开。缺省自动策略下若全部收起，展开首个有效组；不能覆盖用户明确设置或后续手动操作。详情不套用必填折叠策略，查询重置遵循查询专项。

## 5. 快捷填写

- 新增编辑每次打开默认关闭。先应用 UI 及全部显隐，再排除不提交组，基于该集合判断：可填写字段超过 7 个，必填项为 1～4 个，且不存在明确复杂对象、对象集合或其扁平子字段时允许启用。不限制原分组数。
- 禁用/只读项不计数；`allowInput=false` 的可选择下拉仍可填写。复杂性依据 `complexValue`、`complexGroupKey`、复杂映射及 JSON 声明，不分析当前值或收起后的 DOM 猜测。
- 开启后稳定前置可填写必填项，其余字段维持相对顺序放在细分隔线后的“更多”，默认收起；使用向下箭头“更多”及向上箭头“收起更多”。不提交组字段完全不出现、不计数。
- 暂时隐藏普通分组外壳及组控制，保留数据、提交选择、显式跨度和输入契约；允许临时忽略分组引入的起始新行。不得按字段语义重新生成组件、分组或校验。
- 更多收起不等于不提交。校验失败展开更多并定位。关闭或资格失效时恢复原普通模式顺序、组状态和输入，不把快捷模式的错误展开写回普通模式状态。

## 6. 详情、长内容与布局后处理

- “展示空值”默认开启：先确定场景、权限、UI 显隐及类型展示资格，再执行该设置。关闭时隐藏 null、undefined、空白字符串、空基础数组、显式 JSON 的空对象/数组及其序列化形式；保留 0、false。开启不补造接口未返回字段，不恢复隐藏字段。过滤后再处理空组/单组，不影响新增编辑提交。
- 实际容器尺寸以 UI 设置生效后为准。按明确布局做列数回落、跨度收敛、自然换行及滚动，不重算业务顺序、分组意图或视觉权重，不改写设置；默认尺寸基准见《前端页面开发规则》。
- 长文本、JSON、大量标签和附件按明确展示契约提供适量摘要及完整查看入口，实际溢出时才增加展开控制，不把全文堆进 hover。JSON 详情优先紧凑只读预览及独立查看器，不要求全文无滚动文本域撑满表单。
- 展示截断不得截断输入或保存数据；图片预览、附件下载等继续执行专项契约。内嵌对象列表按实际数量、最多五条数据行高度展示，超出内部滚动，不分页、不截断集合。

## 7. 远程选项与联动

- 开发时明确依赖、选项来源、提交类型和上下文，运行时只允许当前最新请求、当前记录、当前上下文的响应更新界面，避免旧响应污染。
- 上级变化后重载下级选项，确认旧值失效才清除并提示重选；加载中/失败不是失效，不自动选第一项。判定失效须有完整有效值校验依据，不能只因搜索或分页结果暂未包含原值而判失效。
- 失败保留输入和原选择，提示重试。本次参与提交的受影响依赖尚未核验完成或加载失败时阻止保存；已排除字段/不提交组不阻塞，不受影响字段可继续填写。
- 远程响应、选项标签与展示模式变化不应被当作用户业务值修改。

## 8. 保存、关闭与子表单

- 同一表单保存中只允许一个请求，显示加载状态，防止连点、回车等重复触发；失败恢复操作、保留输入、显示原因，不自动关闭。
- 可写弹窗禁止点击遮罩关闭。回显及默认值初始化完成后，保存独立业务值快照；手动关闭或会放弃输入的离开动作比较当前业务值，有修改则提示放弃，用户取消后保持窗口。改后恢复原值不提示。保存成功按实际接受结果建立新基准或关闭。
- 比较不以“发生过输入事件”为依据。纯折叠、快捷模式、UI 显隐不计入业务值变化，也不能清除先前输入及其修改记录。数组顺序、富文本、文件值、空字符串与 null 按控件/接口契约比较，不做无依据等价转换。
- 保存失败后的手动关闭仍执行比较；`failAction` 或 `Close` 不能绕过保存失败不关闭规则。成功动作不重置 UI 设置。
- 子表单以独立副本编辑，确定且校验通过才回写父表单，父表单保存才提交后端。取消不回写，有修改按快照确认；失败不关闭、不丢输入。

## 9. 提示、焦点与验收

- 字段标题和控件已清楚时，不重复堆积占位、帮助文字和说明条。格式示例、特殊限制及影响提交语义的信息才按需提示；关键约束不可只藏在悬浮提示。
- 错误就近显示；能明确对应字段的服务端错误在字段旁显示，否则表单级提示。校验失败展开并定位，不清空输入或重置整张表单。
- Tab 顺序跟随最终展示顺序，隐藏项不进入焦点；快捷模式同步更新。内容表单回车不误保存，多行文本正常换行，查询回车遵循查询专项。
- 开发完成须检查新增、编辑、详情的实际效果，覆盖正常/窄屏、少/多字段、长内容、UI 设置变化、空组/单组/多组、快捷与不提交组、失败和关闭。数字合规不等于验收通过；不协调、过宽过空、过窄过长、拥挤或按钮不可达时须调整复查。
- 核对真实请求，确认已排除及不提交字段不进入载荷/强制更新，必要标识保留，折叠与快捷模式不扩大范围。浏览器验证方式遵循《前端模块开发规则》；局部、模拟、文档验证不能宣称真实业务端到端验收。
