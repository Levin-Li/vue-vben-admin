# 全局组织与用户选择器

底层 `UserOrgSelector` 保持通用：只加载候选、保存选中记录并发出事件。调用方通过 hook 或包装层决定候选来源、默认值、显隐、是否允许清空和选中校验。

全局包装层规则：管理员和超级管理员可清空；普通用户只有一个候选时自动选择并隐藏；普通用户有多个候选时默认选择第一项且不可清空；没有候选时隐藏且不注入上下文。全局上下文仅作为请求默认范围，查询面板存在显式值时按覆盖规则处理。
