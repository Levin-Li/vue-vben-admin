# 前端页面开发规则

1. 查询表单字段 label 和组件要并排显示，不要上下排列。

2. 页面实现过程中，应优先参考 `playground/src/views`,`docs/src/` 中 components, demos 和 examples 的页面结构、组件组合、间距、交互与代码组织方式。

   图片和文件上传相关规则独立维护在 `前端页面上传规则.md`，涉及上传字段识别、上传接口、回显预览、裁剪、截图粘贴等规则时必须同时遵守该文件。

   表单校验相关规则独立维护在 `前端页面校验规则.md`，涉及字段级校验、跨字段联合校验、集合和复杂对象字段校验时必须同时遵守该文件。

## 公共 CRUD 页面内容区域间距

## 复杂子属性表单规则

- 实体字段可以使用嵌套 VO/DTO 表达稳定的领域结构，例如开票信息、法人信息、联系人信息和邮寄信息；不得为了页面录入便利而破坏实体领域模型。
- 当嵌套对象面向人工录入，且其子字段具有明确业务语义、图片上传、字段级校验或分组展示需求时，页面使用扁平字段，不直接把 `parent.child` 作为通用 CRUD 表单字段；服务端业务保存请求仍保持嵌套 VO/DTO 结构。
- 扁平字段必须按业务语义分组展示，例如“主体信息”“开票信息”“法人信息”“联系人信息”“邮寄信息”“资质图片”；分组应支持展开和收缩，默认展开策略由页面明确配置。
- 每个复杂属性分组应使用“本次提交此数据”勾选控制参数语义：勾选时提交该嵌套属性，不勾选时不提交该属性并提示“本次不变更这部分数据”。原值为 `null` 时默认不勾选且分组默认收缩；原值非 `null` 时默认勾选。展开/收缩只控制展示，不得改变参数提交语义。
- 通用复杂属性分组默认不提供清空操作；已有嵌套属性一旦保存，普通页面不得因取消勾选、收缩分组或清空局部输入而提交 `null` 覆盖服务端数据。确有清空业务时，具体页面必须单独定义权限、明确操作、二次确认和服务端更新语义。
- 页面配置必须显式声明扁平字段与嵌套属性的映射；通用 CRUD 仅在前端回显和提交阶段按该映射转换。控制器、服务端业务请求和实体保持嵌套 VO/DTO 结构，不得为页面录入新增扁平 DTO 或临时组装逻辑。
- 图片、文件、证件照等子字段扁平化后，必须显式配置既有上传组件；不得因为原始实体字段位于嵌套对象内而降级为文本输入或 JSON 编辑器。
- 同一技术字段因上游枚举或其它表单值而具有不同业务语义时，页面必须提供联动的标题、占位提示、帮助文本和格式校验；切换条件后不得继续沿用旧语义的文案或校验。中国居民身份证号须校验出生日期和18位校验码，并兼容15位旧号码。技术字段名、后端请求结构和持久化结构保持稳定。
- 只有以下情况可以直接编辑嵌套对象：原样 JSON 配置、动态 Schema、字段数量/结构不稳定，或业务明确要求整体传递；此时使用 JSON/JsonSchema 编辑器，并在页面配置中说明原因。

- 公共 CRUD 页面不得在其正常内容包装层自行添加默认外围内边距；查询区、列表区等内容应直接使用布局内容区域提供的可用空间。
- 内容区域的外围留白、背景、圆角和边框由“用户界面设置”统一配置；公共 CRUD 组件不得以固定内边距叠加、抵消或替代这些设置。
- 本规则只约束公共 CRUD 页的最外层正常内容包装区，不移除查询面板、表格工具栏、列表面板、表单、弹窗、抽屉等内部间距。
- 个别业务页面确有局部排版需求时，必须在该页面明确的局部容器中声明间距；不得为此修改公共 CRUD 页的零内边距默认值。

3. 页面标题和表单标题应分层处理，不允许直接复用页面标题生成创建、编辑、详情表单标题。

   标题规则：
   - 菜单标题、页面标题、标签页标题、面包屑标题可以保留“管理”后缀，例如：`用户管理`、`字典管理`。
   - 创建表单标题应使用：`新增{业务对象名}`，不带“管理”后缀，例如：`新增用户`、`新增字典`。
   - 编辑表单标题应使用：`编辑{业务对象名}`，不带“管理”后缀，例如：`编辑用户`、`编辑字典`。
   - 详情表单标题应使用：`{业务对象名}详情`，不带“管理”后缀，例如：`用户详情`、`字典详情`。
   - 业务对象名应优先来自页面显式配置、后端实体或返回对象上的 `@Schema.title`、控制器 `@CRUD.title` / `@Tag.name` 等稳定元数据；不得把“去掉管理”作为主要推导规则，只能作为缺少显式业务对象名时的展示兜底。
   - `successAction = ShowForm` 这类结果展示，不应套用页面标题或通用详情标题，应优先使用操作语义标题。
   - `ShowForm` 结果展示标题优先顺序为：
     - `@CRUD.Op.label`
     - `@Operation.summary`
     - 返回对象类型上的 `@Schema.title`
   - 例如 `MFA二维码`、`查看我的MFA二维码` 这类操作，应直接使用操作标题，而不是 `用户详情` 或 `编辑用户`。

4. 所有表单字段必须按统一的业务优先级排序，不允许只按 OpenAPI schema 原始字段顺序展示。

   字段排序优先级：

   | 顺序 | 字段规则 | 备注 |
   | --- | --- | --- |
   | 1 | `tenantId` | 查询表单整体排序基数会额外加 40；列表/表格中固定左侧，建议宽度 150px。 |
   | 2 | `orgId` | 查询表单整体排序基数会额外加 40；列表/表格中固定左侧，建议宽度 150px。 |
   | 3 | 主键字段（`@Id`） | 列表/表格中固定左侧，建议宽度 150px，支持复制；新增/编辑中通常隐藏或按提交规则处理。 |
   | 4 | 名称/编码模糊查询字段 | 查询表单中 `containsName`、`containsCode` 等名称和编码的模糊查找字段，排序优先级仅低于租户和 ID 类字段。 |
   | 5 | 名称类字段 | 包括 `name`、以及字段名等于或后缀为 `title`、`label`。 |
   | 6 | 手机号/邮箱字段 | 手机号优先于邮箱展示；字段位置在编码字段前。 |
   | 7 | 编码类字段 | 包括 `code`、以及字段名以 `Code` 结尾。 |
   | 8 | 类型类字段 | 包括 `type`、以及字段名以 `Type` 结尾。 |
   | 9 | 状态类字段 | 包括 `status`、`state`、以及字段名以 `Status`、`State` 结尾。 |
   | 10 | 时间/日期字段 | `Pay/Refund` 优先，其次 `Create/Add/New`，再其次 `Expired`，再其次 `Update/Modify`，其他时间字段靠后；小于/小于等于类范围条件排在同组后面。 |
   | 11 | 其他 `*Id` 字段 | 普通关联 ID 字段。 |
   | 12 | 普通字段 | 默认位置。 |
   | 13 | 大文本/编辑器字段 | `textarea`、`editor` 等复杂输入靠后。 |
   | 14 | 描述/备注字段 | 包括 `desc`、`remark`、以及字段名以 `Desc`、`Remark` 结尾，最后展示。 |

5. 查询表单字段在排序前需要执行查询场景过滤，避免把不适合查询的字段展示出来。

   查询表单应排除或转换以下字段：
   - 排除通用控制字段：`orderBy`、`orderDir`、`selectColumns`、`pinyinName`、`containsPublicData`、`containsOrgPublicData`、`tenantShared`、`orgShared`、`optimisticLock`。
   - 排除操作日志字段（如 `actionLog`），不展示、不参与查询提交。
   - 排除密码、密钥、图片、文件、CSS、JSON、大文本、LOB、复杂对象、关联 `Fetch` 字段。
   - 组织类字段不应直接按普通输入框展示；如果是用户可查询或可编辑的组织、机构、部门字段，应按“组织树选择规则”处理。只有系统隐含、无需用户选择或由上下文自动注入的组织字段才隐藏。
   - 排除创建时间/更新时间等继承基础字段，除非已经提供明确的范围查询字段。
   - 同一业务字段的列表查询契约同时提供可用模糊参数和精确参数时，查询表单（即查询面板）必须优先展示并提交模糊参数；例如同时存在 `containsName` 与 `name`、`containsGroupName` 与 `groupName` 时，使用前者，不得同时展示或提交后者。模糊参数是否可用，必须以当前列表查询方法、查询 DTO、参数注解或已确认的接口契约为准。
   - 如果不存在可用模糊参数，查询表单才可使用精确参数。业务明确要求精确匹配时，页面静态配置或紧邻配置说明必须声明该例外及业务原因；公共 CRUD 运行时不得按字段名自动推断或替换查询参数。
   - 如果存在 `inXxx` 或 `notInXxx`，则原始 `xxx` 字段不重复展示；`in` 优先级高于 `notIn`。
   - 如果存在 `betweenXxx`，则同一字段的 `lt/gt/lte/gte/in` 类查询不重复展示。
   - 查询表单中，成对出现的“开始时间 / 结束时间”字段应合并成一个时间范围选择器，不分成两个独立输入框。
   - 查询表单中的 `textarea` 统一降级为单行输入，避免查询区高度失控。
   - 查询表单中的 `input-array` 统一降级为 `input-text`，数组值由逗号或换行输入。

6. 所有表单界面都必须遵守静态配置优先原则；新增/编辑表单字段还必须执行场景过滤。
   - 本节规则适用于所有表单界面，包括查询表单、新增表单、编辑表单、详情表单、弹窗表单、抽屉表单、设置表单、子表单、JsonSchema 动态表单和自定义业务表单。字段语义、组件类型、布局宽度、顺序、分组和可编辑性等开发规则都应在页面开发、代码生成或代码审查阶段落实到静态配置或明确组件契约中；公共运行时只执行这些配置和契约。
   - 运行时可以执行明确声明的业务上下文逻辑，例如当前登录用户、当前租户、组织权限、远程选项、后端返回的 schema 或页面显式传入的上下文；运行时不得把这些例外扩大为字段语义推断、自动布局、自动跨列、自动排序或自动改写组件类型。
   - 非展示表单排除只读字段（`@Schema(accessMode = READ_ONLY)`）。
   - 排除操作日志字段（如 `actionLog`），不展示，也不提交。
   - 排除创建时间、修改时间、更新时间字段（如 `createTime`、`lastUpdateTime`、`updateTime`），不展示，也不提交。
   - 新增表单中 `enable`、`editable` 默认值为 `true`。
   - `editable`（是否可编辑）字段在创建表单和查询表单中不受超管限制；在所有编辑表单中，只有当前登录用户为超管时才展示并允许编辑。非超管编辑时必须隐藏该字段，且不得提交 `editable`，也不得把它加入 `forceUpdateFields`。超管身份必须来自当前登录用户信息或后端约定的超管标识，前端不得根据页面、字段或普通角色文案自行推断。
   - 公共 CRUD 仅在查询表单实际渲染 `editable` 查询控件时，才为非平台用户默认设置“是否可编辑 = 是”；判断必须基于最终可渲染查询项，不能只因原始字段元数据包含 `editable` 就向请求注入该参数。查询表单没有该控件时不得设置默认值。
   - 新增/编辑表单忽略 `orgIdList` 和树节点路径 `nodePath` 等不适合直接编辑的系统字段。
   - `tenantId` 属于租户归属字段，创建、编辑表单中仅当前登录用户为超管/平台租户用户时动态展示；展示时必须排在表单第一位。非超管/非平台租户用户不展示该字段，提交值应来自当前租户上下文或后端返回，不允许用户手工改租户归属。
   - `orgId` 以及组织、机构、部门类归属字段，只有后端返回的当前登录用户信息明确表明其为管理员时，才允许在创建、编辑表单中修改；非管理员只能只读展示或由上下文自动注入并隐藏提交。前端不得自行根据角色名、用户名或权限码臆测管理员身份。
   - 乐观锁 `optimisticLock` 在编辑/提交场景应隐藏但保留提交能力。
   - 单行关联操作如果需要 ID，应把 ID 作为隐藏字段加回表单。
   - 文件路径、图标、图片等字符串类型字段，在所有可编辑表单中应由页面开发者或 AI 在开发、生成、审查阶段静态配置为上传组件，不使用普通文本输入。
   - 文件路径、图标、图片等字符串集合类型字段，在所有可编辑表单中应由页面静态配置为支持多文件的上传组件。
   - 本节只定义可编辑表单中上传字段的默认映射边界；上传字段识别、接口、回显、预览、裁剪和粘贴上传细节以 `前端页面上传规则.md` 为准，公共运行时不得按字段名或字段类型临时猜测上传组件。
   - 图片类字段默认应支持裁剪；除非字段或页面明确声明不裁剪。
   - 新增、编辑表单中的字符串字段，如果长度大于 255、属于 LOB、使用 `@Column(columnDefinition = "text")`，或使用 `@JdbcTypeCode(SqlTypes.LONGVARCHAR)`，页面静态配置应使用多行文本组件；正常情况下应通过 `fullRow: true` 或 `span: -1` 独占一整行。只有具体页面存在明确紧凑布局需求时，才允许通过显式 `span` 配置作为局部例外。
   - 复杂集合字段应在页面静态配置中独占一整行，并支持动态添加、修改和删除集合元素；基础字符串集合如果按 tags 或紧凑清单入口展示，应按后续显式 `span`/`fullRow` 规则处理，不由公共运行时按集合类型自动跨列。
   - 字段类型是 `Map`、`Map` 子类、`JSONObject`、`JsonNode`、`ObjectNode` 等 JSON 对象类型，且字段语义不是图片或文件时，页面开发者或 AI 代码生成/维护者应在编写页面配置时静态判断并配置为 JSON 字段，按 JSON 编辑器处理，不按复杂 DTO 展开；公共运行时代码不得再根据字段名、后端类型或值内容自行推断字段是否为 JSON。
   - JSON 字段在可编辑内容型表单中默认不因为 JSON 类型独占一整行，也不因为是 JSON 编辑入口而主动跨列；表单内应展示紧凑入口，默认按普通字段占 1 列，点击后弹出 JSON 编辑器弹窗。这个默认 1 列来自页面配置没有声明跨列时的普通字段通用默认布局，不应由公共渲染层按 JSON 类型另行决定宽度、排序或视觉权重。页面一旦静态显式配置 `fullRow: true` 或 `span: -1`，公共表单渲染层必须尊重该配置并让字段占满一行；新页面只有明确要求把 JSON 编辑器内联展示在当前表单中，或确有大面积 JSON 树、源码、预览等非入口型展示时，才应这样配置，并应有局部例外理由。普通弹窗入口、抽屉入口或只读入口不得为了“JSON 字段”本身放宽宽度。
   - 字段类型是单个复杂 DTO 对象，且不是集合类型时，应独占一整行，并使用独立容器包裹后展开其 DTO 字段，不应只渲染成一个普通输入框。
   - 基础类型集合字段如果元素类型是 `String`，且字段语义是标签、关键字、模块、域名等自由输入型基础字符串列表，应使用 tags 输入组件，支持动态新增、编辑、删除单个字符串项。
   - 角色编码、角色 ID、角色列表等角色选择语义的集合字段，不属于自由输入 tags；应使用角色选择组件，数据来源优先使用角色控制器提供的可授权/可分配角色接口，并按后端字段要求提交角色编码或角色 ID 数组。
   - 基础字符串集合字段如果语义是图片或文件资源列表，应按 `前端页面上传规则.md` 使用多文件或多图片上传组件，不使用普通 tags 输入。
   - 集合字段如果是图片类型，应使用图片上传/图片管理组件。
   - 集合字段如果是文件类型，应使用文件上传/文件管理组件。
   - 图片和文件上传必须通过 `FileStorageController`，不要自行定义上传接口。
   - 集合字段如果元素是复杂 DTO 对象类型，应独占一整行，并默认使用“列表 + 元素编辑器”的方式编辑，而不是直接展开成大块嵌套表单。
   - 复杂 DTO 集合的列表区应展示每个元素的关键字段摘要，例如名称、编码、状态、备注等适合识别该元素的字段。
   - 复杂 DTO 集合必须支持新增元素、编辑元素、删除元素。
   - 编辑单个复杂 DTO 元素时，应通过独立编辑容器完成，例如弹窗、抽屉或子表单容器；编辑完成后回写到当前集合字段，提交时保持数组对象结构不变。
   - 复杂 DTO 集合不应退化为纯 JSON 文本输入，除非页面明确指定使用 Json 编辑器。
   - 复杂 DTO 集合中的关键字段摘要和元素编辑方式，必须由页面实现显式指定，不允许运行时猜测。
   - 字段上存在 `@JsonSchemaEditor` 注解时，优先按该注解声明的逻辑处理组件、schema 和编辑行为，上述默认规则不覆盖 `@JsonSchemaEditor`。
   - `@JsonSchemaEditor.title` 为空时，标题默认取字段 `@Schema.title`。
   - `@JsonSchemaEditor.desc` 为空时，描述默认取字段 `@Schema.description`。
   - `@JsonSchemaEditor.jsonSchema` 支持三种格式：
     - Java 类名，如 `com.test.UserAddress`，前端必须通过 `JsonSchemaController` 获取该类型的 JSON Schema。
     - URL，如 `/sys/jsonSchema/userAddr.json`，前端应按远程 schema 地址加载。
     - 同类属性引用，如 `:attrName`，表示从当前 DTO 同一个对象的属性中读取 schema。
   - 前端获取 Java 类 JSON Schema 必须通过当前模块 API 层调用 `JsonSchemaController.genJsonSchema`。
   - 请求体使用 `{ "typeGenericStr": "Java类名或泛型类型字符串" }`。
   - 接口返回数据中的 `jsonSchema` 字段为表单 schema 内容；结果可按 `typeGenericStr` 缓存。
   - `@JsonSchemaEditor.inlineMode=true` 时，动态表单内联铺开展示。
   - `@JsonSchemaEditor.inlineMode=false` 时，动态表单应通过弹窗、抽屉或子表单容器展示，避免主表单过长。
   - `@JsonSchemaEditor.columns=-1` 表示该 JsonSchema 动态表单组件可按后端 schema、页面静态配置、容器宽度和视口做机械响应式列数收敛；列数计算不得扩展为公共 CRUD 表单按字段名、字段类型或字段值重新推断布局意图。自动列数不能超过父表单布局允许的最大列数，也不能超过项目规则的 5 列上限。
   - `@JsonSchemaEditor.columns>0` 时，优先使用注解指定列数，但仍不能超过父表单布局和项目 5 列上限。
   - JsonSchema 动态表单本质上仍是结构化表单，不是代码编辑器或原始 JSON 编辑器。无论它出现在查询、新增、编辑、详情表单内，还是出现在租户设置值编辑、快捷编辑、查看值弹窗、抽屉或子表单容器中，只要最终渲染为多个可读写字段，就必须遵守本节所有表单界面的静态配置优先、等宽列、视觉权重、`maxWidth`、普通字段单列不超过 `480px`、外壳不超过页面内容区 `80%` 等规则。
   - JsonSchema 动态表单外层弹窗或抽屉不得仅因为字段来源是 JSON Schema、`valueType=Json` 或字段内容可序列化为 JSON，就使用代码编辑器/原始 JSON 编辑器的大宽度策略，例如固定 `86vw` 或 `1480px`。只有实际展示的是源码、代码、原始 JSON 文本、大面积 JSON 树、表格型配置、媒体预览或页面明确声明需要大面积编辑时，才允许按特殊编辑器放宽宽度，并应有局部例外理由。
   - 字段数量很少或视觉权重较低的 JsonSchema 动态表单应优先降低列数和外壳 `maxWidth`，避免为了填满宽弹窗而横向拉大输入框或制造大面积空白。典型的 1 至 2 个普通字段表单不应铺满宽屏，应按紧凑内容型表单处理。
   - JsonSchema 动态表单编辑复杂字段或复杂集合元素时，每个元素应支持新增、修改、删除；提交时保持对象或数组原始结构，不应转成逗号字符串。
   - 所有表单界面都应支持按信息域分组排布。分组用于指导具体页面实现，不能演变成运行时根据字段名猜测的自动生成逻辑。
   - 本节布局规则是页面开发阶段的配置指导，用来帮助开发者决定字段顺序、分组、`fullRow` 和 `span` 等页面配置；不是运行时覆盖机制。所有公共或专用表单渲染层都必须尊重页面已经显式声明的布局配置，不能因为开发规则推荐默认布局就忽略具体页面的明确意图。
   - 字段语义、组件类型和布局意图必须在页面开发、代码生成或代码审查阶段由开发者/AI 依据后端类型、字段名、注解、业务语义和上下文完成判断，并写入静态页面配置。公共运行时只消费这些静态配置，不再重新判断“这个字段是图片、日期、JSON、上传、长文本还是普通文本”，也不根据这种判断改变宽度、顺序或分组。
   - “同类型、同高度组件尽量相邻”“JSON 默认普通列”“复杂编辑器可独占一行”等规则，都是页面配置编写和代码审查规则。它们应通过调整页面字段顺序、`layoutGroup`、`layoutOrder`、`layoutNewRow`、`span`、`fullRow` 等静态配置落地；公共运行时不得为了满足这些规则按组件类型自动重排、自动跨列或自动改写视觉权重。
   - 允许的运行时逻辑必须是明确声明的业务上下文或权限逻辑，例如 `tenantId` 根据当前登录用户、当前租户、超管/平台租户身份决定是否展示和可选范围，`orgId` 根据当前登录用户管理员身份决定是否可编辑，远程选项根据页面显式配置的 `loadOptions` 或接口参数加载。此类例外必须在规则、页面配置或专用组件契约中写清楚依据和边界；不得借“运行时例外”去做字段语义猜测、自动布局、自动跨列或自动重排。
   - 表单分组应由具体页面显式配置，例如 `layoutGroup`、`layoutOrder`、`layoutNewRow`、`span`、`fullRow`，公共组件只执行配置结果，不自行根据 `logo`、`image`、`icon` 等关键词推断分组。
   - 表单字段显式配置正数 `span` 时，公共组件必须优先执行该跨列配置，不得再被字段类型或历史默认布局覆盖。`tags`、`string-array`、`json`、上传等字段只有页面配置了 `fullRow: true` 或 `span: -1` 时才独占整行；满足长文本规则的 `textarea` 字段在新增、编辑表单中正常应静态配置为独占整行。页面配置了 `span: 2` 就占据两列；页面没有配置跨列时就按普通默认列处理。
   - 通配符清单编辑器应采用清单行内添加：新增输入框和添加按钮是清单中的第一行，已有规则逐行展示并提供图标化复制、编辑、删除；组件外围保留轻量边框，空状态文案使用“暂无内容”，默认不展示无数据图标，只有页面显式配置 `showEmptyImage: true` 时才展示图标。右侧测试和复制 JSON 图标应纵向排列并位于新增输入行分隔线下方，测试操作使用能表达“测试/试验”的图标，不使用放大镜类图标。如果字段没有配置 `help` 或备注说明，编辑器应自动显示“支持\*和?匹配”的默认提示；字段已有说明时不得重复显示。通配符清单编辑器在表单中至少应占据两列宽度，允许和其它短字段同排；如果页面密度、文案长度或视觉平衡需要，也可以通过 `fullRow: true` 或 `span: -1` 独占整行。选择两列还是整行应以表单网格规整、字段节奏协调为准，不应为了强行分组制造不必要空白。
   - 推荐分组顺序为：归属信息、基础信息、媒体资源、内容信息、业务配置、扩展信息、备注信息、审计信息。
   - 归属信息包括 `tenantId`、`orgId`、`ownerId`、`parentId` 等字段。
   - 基础信息包括名称、编码、类型、状态、手机号、邮箱等核心识别字段。
   - 媒体资源包括 Logo、头像、图标、图片、大图、封面、二维码等字段；多个图片或文件字段应尽量放在同一组，并尽量同一行展示。
   - 内容信息包括详情、描述、内容、简介、富文本、JSON 配置等字段；大文本、复杂对象、集合对象仍按既有规则独占一行；普通 `type: 'json'` 字段统一通过独立弹窗编辑，表单内只展示只读输入入口，不能直接输入文本，不再展示外置“编辑”按钮。开发新页面时，普通 JSON 字段默认按 1 列处理，不应无理由配置 `fullRow`、`span: -1` 或正数 `span` 来放大入口；页面已显式配置 `fullRow: true` 或 `span: -1` 的 JSON 字段，公共组件必须按独占一行执行。多个普通 JSON 入口需要协调排布时，应依靠字段顺序和同类聚合相邻展示，而不是放大宽度。

### 设置类内容值独立编辑规则

本规则适用于设置、界面设置、用户设置、租户设置、服务插件设置等“设置元数据与内容值分离”的页面。典型字段包括 `editor`、`configEditor`、`valueContent`、`value`、`detailInfo` 等，其中编辑器字段决定内容值的 JSON Schema、JSON、代码或其它结构化编辑方式。

- 新增和普通编辑表单只维护设置元数据，例如名称、编码、作用域、类型、编辑器、启用状态、排序和备注；不得直接展示、修改或提交内容值字段。
- 新建记录保存成功后，用户通过列表行操作“编辑值”维护内容值；未保存的记录不得提供内容值编辑入口。
- 页面必须提供“查看值”和“编辑值”两个独立行操作。查看值只读；编辑值使用弹窗、抽屉或其它独立可写容器，禁止在列表页或普通编辑表单内直接编辑。
- 编辑值时，必须以当前记录的 `editor` / `configEditor` 作为唯一编辑器来源；类名、URL、内联 JSON Schema 和普通 JSON 的处理继续遵循 JsonSchema 编辑器规则。
- 内容值为 JSON Schema 动态表单时，弹窗内字段布局、列数、最大宽度、校验、对象/数组提交结构，继续遵循本规范的 JsonSchema 表单规则。
- 保存内容值必须复用该实体已有的普通更新接口，例如 `Xxx/update`。请求只携带主键、乐观锁、内容值，以及默认更新机制需要的强制更新字段标记；不得为 `valueContent`、`value`、`detailInfo` 等字段新增专用更新接口。
- “编辑值”按钮的权限必须从实际调用的既有 `update` API 派生；若记录不可编辑，按钮不得展示。
- 内容值编辑成功后，应关闭编辑容器并刷新当前列表；普通编辑表单不得因内容值存在而回填、覆盖或清空其值。
- 本节是普通 JSON 表单入口规则的明确例外：设置类内容值使用列表行操作，不在普通表单内渲染只读 JSON 入口或额外编辑按钮。
  - 业务配置包括排序、是否启用、是否可编辑、开关类字段、等级、策略类字段；开关类字段应尽量放在同一行。
  - 扩展信息包括 Map、复杂 DTO、集合对象、扩展 JSON 等字段。
  - 备注信息包括备注、说明等字段。
  - 审计信息包括创建时间、更新时间、操作日志等字段；所有可编辑表单中仍按既有规则隐藏，不展示、不提交。
  - 同一分组内字段应优先连续展示，尽量不要被其他分组字段插入。
  - 当某个分组需要避免被上一组字段挤占行空间时，页面应显式配置该组首个字段从新行开始，例如 `layoutNewRow: true`。
  - 页面开发和审查时，多列表单应优先在同一分组内补位，减少明显空缺；可以通过调整页面字段顺序、`layoutOrder`、`layoutNewRow` 等静态配置，把后面同组、同尺寸字段提前补到空位。为了充分利用横向空间，每一行都应尽量排满，不应在前面行留下可避免的空位后把同组、同尺寸或短字段放到下一行。本规则不允许运行时自动重排字段，也不允许通过拉宽普通字段、临时改变 `span`、突破 `maxWidth`、跨分组强行提前字段，或套用到查询面板来填空。
  - 不建议为了填满空位跨分组强行提前字段，避免语义顺序混乱。
  - 表单排版的总体优先级必须固定为：第一满足业务，第二协调美观，第三空间利用。业务语义、权限、校验、字段含义和页面显式布局配置优先级最高；在业务正确的前提下，再追求字段高度接近、列宽稳定、视觉重心均衡、界面整齐美观；最后才考虑尽量排满每一行、减少空白。不得为了空间利用牺牲业务语义或把高度/语义明显不协调的字段硬凑在同一行。
  - 当字段之间没有明确业务排序约束时，同类型、同高度、同交互形态的组件应尽量排在同一行，例如多个数字输入、多个开关、多个短文本输入或多个同尺寸选择器。页面配置应通过字段顺序、`layoutOrder`、`layoutNewRow`、`span` 等显式手段达成这个效果，避免同类短字段被高组件或跨列组件打散。
  - 表单字段排序规则仍是基础约束；分组排布是在字段排序后的二次组织，不能覆盖隐藏、校验、上传、集合对象、复杂对象等既有规则。
  - 在详情、创建、编辑这三类表单中，同一表单内各列的列宽应保持一致，不允许出现某一列明显更宽、另一列明显更窄的随意布局。
  - 当表单使用多列布局时，应采用等宽列布局。2 列时两列等宽，3 列时三列等宽，4 列时四列等宽，5 列时五列等宽；只有字段被规则明确要求独占一行时，才允许跨列。
  - 桌面宽度下的 CRUD 弹窗表单应优先利用可用横向空间，普通短字段可以 4 个一排；不要在宽弹窗中把字段长期堆在左侧并让右侧大面积空白。通配符清单编辑器这类两列字段应尽量和另一个两列字段或短字段自然拼行，只有布局更规整时才独占整行。
  - 详情表单、创建表单、编辑表单在同一页面模块中，应尽量保持一致的列宽策略和整体宽度策略，避免三者视觉结构差异过大。
  - 详情表单用于只读信息展示时，字段项应采用左右结构，即左侧为字段名称，右侧为字段值；不要沿用创建/编辑表单的输入框样式，也不要把 label 和 value 上下堆叠。
  - 详情表单中的左右结构仍应遵守等宽列、最大列数、最大宽度、独占行和分组规则；字段值较长时应在右侧区域内换行或折叠展示，不应破坏整体列宽。
  - 详情表单中的复杂类型属性默认不展示，避免把未知对象、复杂 DTO、复杂对象集合、Map 或嵌套结构直接铺到详情页。只有字段在页面开发/代码生成/审查阶段已根据后端元数据、页面配置或明确类型规则配置为 JSON 对象，或集合元素是字符串、数字、布尔、枚举、日期时间等基础类型时，才允许在详情页展示；公共运行时不得临时猜测未知复杂对象应该如何展示。
  - 详情表单中展示 JSON 对象时，默认使用只读普通文本域展示序列化后的 JSON 内容；该文本域不应设置内部滚动条，也不应默认启用代码编辑器或 Json Viewer。文本域旁边应提供“查看”或“查看 JSON”按钮，点击后再打开 Json Viewer 弹窗、抽屉或预览层。
  - 详情表单中的基础类型列表可以按标签、逗号分隔文本或只读列表展示；列表元素一旦是复杂对象，默认不展示，除非页面显式配置摘要字段、子表格或 Json Viewer 展示方式。
  - 详情表单中凡是枚举、字典或固定可选值字段，都必须展示对应标签文案，不直接展示原始枚举名、字典编码、选项值或 ID。基础类型列表中的元素如果属于枚举、字典或固定可选值，也应逐项转换为标签后展示。
  - 枚举标签应来自枚举接口、后端注解或页面配置中的枚举选项；字典标签应来自字典接口或明确配置的字典项；固定可选值标签应来自 `@Options.items`、页面配置或其它明确选项来源。无法拿到标签时可以临时用原值兜底，但不应把原值展示作为默认设计。
  - 所有表单界面的列数不能只按字段数量决定；页面开发或 AI 审查时还应按字段视觉权重、预估高度和目标视口共同估算，并把结论落实到页面字段顺序、`maxFormColumns`、`span`、`fullRow`、`layoutNewRow` 等静态配置中。
  - 页面开发阶段的字段视觉权重建议为：普通输入、下拉、数字、日期为 `1`；开关为 `0.6`；图片、文件上传、textarea 为 `2`；内联 JSON、复杂对象、集合列表为 `3`。普通 JSON 弹窗入口按普通字段估算，不因为类型是 JSON 自动提高权重。
  - 页面开发阶段估算表单总视觉权重时，`<=6` 可优先 1 列，`>6 且 <=10` 可优先 2 列，`>10 且 <=18` 可优先 3 列，`>18 且 <=30` 可优先 4 列，`>30` 可优先 5 列，但最终列数仍不能超过项目 5 列上限。公共运行时可以根据页面静态配置、容器宽度和视口做机械响应式收敛，但不得按字段类型重新推断视觉权重或布局意图。
  - 新增、编辑、详情、设置、弹窗、抽屉、子表单等内容型表单受本节 5 列上限约束；查询表单不适用该上限，查询表单列数按公共 CRUD 查询表单布局规则执行，但仍必须遵守静态配置优先、字段语义不运行时猜测、显式配置优先等原则。
  - 如果表单中存在 2 个及以上图片或文件字段，应优先按至少 3 列排布，让媒体字段有机会横向聚合展示。
  - 内容型表单的最小视觉行数为 3 行。若按当前列数排布后少于 3 行，应优先降低列数补足到至少 3 行；查询表单不适用该规则。
  - 如果按当前列数预估表单高度超过视口高度的 `75vh`，应优先增加列数，直到高度更协调或达到最大列数。
  - 如果增加列数会导致普通字段单列宽度小于合理值，例如 `240px`，则不继续增加列数，改为保持当前列数并允许弹窗内部滚动。
  - 表单整体视觉比例应尽量接近 `宽 : 高 = 1 : 1.2 ~ 1 : 1.6`，避免出现明显“窄而长”或“宽而空”的布局。
  - 页面开发和审查时，图片、上传、大文本、复杂对象、集合对象必须参与视觉权重估算；不能只按字段个数估算表单高度。估算结果应体现在页面静态配置中。
  - 所有表单外壳组件的宽度限制语义是 `maxWidth`，不是固定 `width`。
  - 这些 UI 外壳组件最多不超过页面内容区域宽度的 80%，并受页面或公共组件定义的 `maxWidth` 限制。
  - 内容型弹窗整体高度最多不超过视口高度的 90%。当表单、详情或编辑器内容超过该高度时，应让弹窗内容区出现纵向滚动条，底部操作按钮仍保持可达；不得让弹窗整体突破视口或把按钮挤出屏幕。
  - 新增、编辑、配置录入、子表单元素编辑、JSON/代码/表达式编辑器等承载用户输入的可写弹窗必须使用阻断式模态交互，默认禁止点击遮罩关闭，避免误触导致已填写内容丢失；关闭应来自明确的取消、关闭、保存或确认动作。只读查看、预览、图片/视频/二维码展示等不承载未保存输入的弹窗可以按场景保持轻量关闭。
  - 所有内容型表单的弹窗和表单容器应占满当前弹窗可用宽度，不再按内容自然收窄成窄长弹窗。
  - 内层表单不要再额外设置“80% 宽度”之类的二次收窄规则；外层 UI 外壳已经限制最大宽度，内层表单应使用当前外壳的全部可用宽度。
  - 在所有内容型表单中，普通字段的单列宽度不应超过 `480px`。
  - 内容型表单必须有统一的内容网格边界。普通单列字段、显式 `span` 字段和 `fullRow`/`span: -1` 独占行字段都应落在同一套表单内容网格内；不得让普通字段在单元格内二次居中，而独占行字段占满弹窗额外留白，导致左右边距不一致。
  - 文本输入框、文本域、下拉、日期、数字等普通字段应填满自己被分配到的网格列；普通字段 `480px` 上限应通过表单内容网格和列宽控制实现，不应让字段组件在已分配列内再缩成局部短输入框。独占一行字段可以占满表单内容网格，但不能突破该网格去占满弹窗两侧为整体居中预留的空白。
  - 内容型表单弹窗宽度默认按最终列数选择递增档位，重点保证高列数表单不会套用明显低列数的弹窗宽度；不要求在所有视口和所有特殊场景下机械满足固定比例关系。
  - 弹窗宽度推荐下限按列数分层：1 列外壳约 `560px`，内容列目标约 `480px-500px`；2 列约 `960px`，3 列约 `1120px`，4 列约 `1280px`，5 列约 `1480px`。这些数值是外壳 `maxWidth` 的推荐下限，仍受页面内容区 `80%`、视口和明确特殊场景约束；若页面显式配置更大宽度可以保留，显式较小宽度只能用于低列数或有局部例外理由的特殊表单。
  - 如果当前视口或页面内容区 `80%` 上限无法承载某列数对应的推荐宽度或最小合理单列宽度，应先按可用宽度回落到更低列数，再按回落后的最终列数计算弹窗宽度；不得保留 4 列布局却套用 3 列或更小的弹窗宽度。
  - 页面宽度增大时，应保持外层容器 `maxWidth`、列宽一致和普通字段单列宽度不超过 `480px`，不要继续横向拉大输入框，也不要在两侧留下大面积空白。
  - 当页面可用宽度较小时，应优先减少列数；优先从 5 列降到 4 列，再从 4 列降到 3 列，再从 3 列降到 2 列，必要时降到 1 列。
  - 这套 `maxWidth` 规则适用于详情、创建、编辑表单外层容器，不适用于查询表单、表格、图片预览弹窗、代码编辑器等特殊区域。
  - 对于独占一行的字段，例如大文本、上传区、子表单、复杂对象集合，可以占满表单可用宽度，但仍应受外层容器 `maxWidth` 限制，不适用普通字段单列 `480px` 上限。普通 JSON 弹窗入口默认不适用本条；但只要页面静态配置了 `fullRow: true` 或 `span: -1`，公共组件必须将该 JSON 字段作为独占一行字段处理。新配置应优先用于内联 JSON 编辑器、大面积 JSON 树、源码、预览等非入口型内容，并应有局部例外理由。
  - 如果某个页面因业务需要必须突破 `maxWidth` 或采用非等宽列布局，应有明确例外理由，且应尽量局部化，不作为默认实现。

7. 组件类型选择必须按字段语义映射为合适的前端组件。

   | 字段/类型规则 | 组件语义 | 本地 Vue 推荐组件 |
   | --- | --- | --- |
   | 默认字符串 | 单行文本 | `Input` |
   | 字符串长度大于 255、LOB、`@Column(columnDefinition = "text")` 或 `@JdbcTypeCode(SqlTypes.LONGVARCHAR)` | 多行文本 | `Input.TextArea` |
   | 数字 | 数字输入 | `InputNumber` |
   | 布尔 | 开关 | `Switch` |
   | 枚举 | 下拉选择 | `Select` |
   | 日期时间 | 日期时间选择 | `DatePicker` 或日期时间组件 |
   | 日期 | 日期选择 | `DatePicker` |
   | 时间 | 时间选择 | `TimePicker` |
   | 范围日期时间 | 日期时间范围选择 | `RangePicker` |
   | 省/市/区 | 城市/地区选择 | 级联选择或城市选择组件 |
   | 租户 ID | 远程租户选择 | 远程 `Select` |
   | 组织 ID / 组织 ID 列表 | 授权组织树选择 | `TreeSelect` |
   | ownerId | 远程用户选择 | 远程 `Select` |
   | JSON | JSON 编辑 | 表单内紧凑入口 + JSON 编辑器弹窗，必须校验 JSON |
   | 图片 | 图片上传 | 图片上传 |
   | 文件 | 文件上传 | 文件上传 |
   | HTML | 富文本编辑 | 富文本编辑器 |
   | CSS | CSS 代码编辑 | 代码编辑器 |
   | 密码 | 密码输入 | `Input.Password` |
   | 颜色 | 颜色选择 | 颜色选择器 |
   | URL | 单行文本 + URL 输入体验 | `Input` + 非阻断提示；阻断式校验以校验规则为准 |
   | 手机号 | 单行文本 + 手机号输入体验 | `Input` + 非阻断提示；阻断式校验以校验规则为准 |
   | 邮箱 | 单行文本 + 邮箱输入体验 | `Input` + 非阻断提示；阻断式校验以校验规则为准 |
   | 身份证 | 单行文本 + 身份证输入体验 | `Input` + 非阻断提示；阻断式校验以校验规则为准 |
   | 基础类型集合 | 数组输入 | 多行文本或 tags/select，提交时转数组 |
   | 子表单/对象集合 | 子表单或分组表单 | 子表单、分组表单或抽屉/弹窗编辑 |
   | 带 `@JsonSchemaEditor` 注解的字段 | JsonSchema 动态表单 | 按注解配置渲染 JsonSchema 动态表单 |

   文件上传规则：
   - 前端文件、图片上传必须通过 `FileStorageController`。
   - 上传接口路径由当前模块 API 层提供，不应在页面规则中写死某个模块的 API 前缀。
   - 单文件上传接口优先使用 `FileStorageController.uploadSingleFile`。
   - 多文件上传使用 `FileStorageController.uploadFiles`。
   - 获取 OSS 上传凭证使用 `FileStorageController.getOssUploadCredential`。

   行政区划选择规则：
   - 行政区划字段识别必须在页面开发、代码生成或代码审查阶段按稳定字段元数据完成，不得由公共运行时仅凭字段名临时猜测。字段名严格等于 `areaCode` 或 `regionCode` 时，必须识别为行政区划字段；字段的 `@Schema.title` 或 `@Schema.description` 包含“区域编码”“行政编码”“城市编码”“区县编码”任一关键词时，同样必须识别为行政区划字段。
   - 已识别的行政区划字段，以及省、市、区/县、街道、行政区划、地区编码等构成的字段组，应优先判断其是否构成行政区划选择块，不应分别当作普通输入框或普通下拉框处理。
   - 行政区划选择块应优先使用项目已有的省市区/级联选择组件；如果项目没有现成组件，应封装一个统一组件，不能在每个页面重复写散乱的省市区下拉。
   - 省、市、区/县之间必须支持联动：选择省后加载或过滤城市，选择城市后加载或过滤区县；上级变化时应清空不再合法的下级值。
   - 提交时应按后端 DTO 字段要求回填，例如分别提交 `provinceCode`、`cityCode`、`districtCode`，或提交统一的 `areaCode`、`regionCode`；不能只提交前端组件内部数组值。
   - 行政编码来源值允许为 2 至 6 位纯数字；不足 6 位时在尾部补 `0` 后查找、回显和提交。少于 2 位、超过 6 位或包含非数字字符时，必须显示自动消失的错误提示，且不得作为有效行政区划提交值。
   - 回显时应根据已有编码或 ID 反推出完整路径，保证列表、新增、编辑和详情页面均正确显示中文省市区层级；单一 `areaCode` / `regionCode` 默认展示完整中文路径，拆分的省、市、区县编码字段展示对应层级中文名称。
   - 如果字段同时包含编码和名称，编码字段作为提交值，名称字段仅用于展示或由后端返回，不应让用户手工维护名称。
   - 查询表单中同样应使用行政区划级联选择；查询提交时可按后端查询参数要求提交最末级编码，或同时提交省/市/区编码。
   - 如果后端字段只有单个 `areaCode` 或 `regionCode`，则组件应支持任意层级选择，并以当前选中层级编码作为提交值。
   - 无法在当前有效行政区划数据中识别的历史六码编码，列表和详情必须原样展示，编辑时不得因回显失败自动清空或改写。

   组织树选择规则：
   - 对于组织 ID、机构 ID、部门 ID 等组织归属类字段，应优先使用树形选择器，不应渲染为普通文本框或普通平铺下拉框。
   - 典型字段包括 `orgId`、`organizationId`、`institutionId`、`deptId`、`departmentId`、`parentOrgId`、`ownerOrgId` 以及中文语义为组织、机构、部门、网点所属组织的字段。
   - 组织类字段是否可编辑必须受当前用户身份控制：是否为管理员以 `RbacController` 当前登录用户信息接口返回的数据为准；管理员可修改，非管理员不可修改，只能只读展示或隐藏后按上下文提交。前端不得硬编码角色名或自行推断管理员身份。
   - 组织树选择器的数据来源默认使用 `RbacController` 的“获取授权的组织列表”接口，前端应通过当前模块 API 层封装调用，默认接口语义为 `/rbac/authorizedOrgList`。
   - 组件形态应使用可搜索、可展开/折叠的 `TreeSelect` 或等价树形选择组件；展示文本优先使用组织名称，节点值提交组织 ID。
   - 单值字段默认单选；集合字段或带 `In` 查询语义的组织字段应使用多选树，并按后端参数要求提交 ID 数组。
   - 新增、编辑、详情表单回显时，应根据组织 ID 回显完整组织名称；如果接口返回树路径，可展示层级路径，避免只显示裸 ID。
   - 查询表单中的组织类字段也应使用组织树选择器；提交时使用字段对应的后端参数名，例如 `orgId`、`inOrgId`、`orgIdList`，不提交组件内部节点对象。
   - 如果字段上存在明确的 `@Options`、业务接口或页面配置指定了其他组织数据来源，可以覆盖默认授权组织列表接口；否则以授权组织列表接口为默认来源。

     脚本和表达式输入规则：
     - 涉及可保存、预览、调试或试运行的 Groovy、SpringEL、JavaScript、SQL 片段、模板表达式和规则引擎脚本时，必须同时遵循根仓 `20-安全开发规则.md`。前端只可根据当前用户的超级管理员身份隐藏或禁用高风险入口；后端保存与执行校验仍是唯一安全边界。
     - 在线测试、调试、预览或试运行请求不得直接携带可执行代码正文；页面只能引用后端已保存且已通过权限校验的配置，并传递运行参数、上下文参数或测试数据。非超级管理员不得获得保存或执行高风险内容的交互入口。
     - 对于 Groovy、SpringEL、JavaScript、`visibleOn`、权限/数据范围表达式、动态显示条件等脚本类输入，应尽量在输入组件附近提示当前脚本上下文可用变量名称。
     - 可用变量必须来自后端注解、Java 源码、接口说明或当前页面明确传入的上下文，不允许前端凭字段名随意猜测。
     - 提示文案应尽量贴近输入框、textarea 或代码编辑器，可使用 help 文案、提示条、tooltip、placeholder 或编辑器说明区，避免用户离开当前表单再查文档。
     - 如果不同脚本类型可用变量不同，应随脚本类型联动变化。例如 Groovy 和 SpringEL 的可用变量不完全一致时，切换类型后提示也要同步调整。
     - 如果脚本有固定前缀、语法约定或特殊对象，也应一并提示。例如 Groovy 表达式需要 `#!groovy:` 前缀时，应明确提示前端是否会自动补前缀，以及脚本中可用的变量名。

     表达式类字段规则：
     - 类似 `tenantMatchingExpression`、`orgScopeExpression`、`visibleOn`、`verifyExpression`、权限表达式、数据范围表达式等字段，不应按普通字符串输入框处理，应先识别表达式语义、表达式类型和值域规则。
     - 如果后端源码中定义了固定特殊值，前端展示时必须转换成业务文案，不直接展示魔法值。例如 `_DEFAULT_TENANT_` 显示为 `默认租户`，`*` 显示为 `所有租户`，空值如果有业务语义则显示为对应文案。
     - 编辑时如果表达式支持多种模式，应拆成“模式选择 + 内容输入/选择”。固定值模式不需要额外输入；远程对象模式使用对应远程选择组件；路径表达式使用文本输入；脚本表达式使用脚本输入并提示前缀和可用变量。
     - 如果表达式有固定前缀，前端必须明确提示该前缀由用户手动输入还是由前端保存时自动补齐。
     - 列表、详情、创建和编辑表单中的表达式展示应保持一致的语义转换，不能一个位置显示业务文案，另一个位置显示原始魔法值。
     - 表达式类型字段与表达式内容字段必须联动。固定模板表达式可以只读或自动填充表达式类型和表达式内容；只有选择自定义表达式时，才允许编辑表达式类型和表达式内容。
     - 表达式字段提交时必须转换回后端字段原始结构，不提交前端 UI 模式值。例如前端内部模式是 `default`、`all`、`groovy`、`path`、`tenant`，提交时仍应转换为后端要求的表达式字符串。
     - 表达式字段的候选值、特殊值、默认值和变量说明，必须来自后端源码、注解、枚举、接口说明或明确业务规则，不允许运行时按字段名猜测。
     - 如果表达式字段允许被清空，且后端默认更新策略会忽略空值，则保存时必须配合 API 规则中的 `forceUpdateFields`。

8. 第三方 OAuth 授权必须通过 `OAuthController`，不要在前端自行拼第三方平台授权地址。
   - 获取支持的第三方平台列表：`GET /oauth/getSupportedPlatforms`。
   - 发起第三方平台授权：`GET /oauth/authorize/{platform}`。
   - 授权回调地址：`GET /oauth/callback/{platform}`。
   - 发起授权时可传 `callbackUrl`，用于授权完成后回到前端指定页面；如果不传，后端会优先使用当前请求来源页面。
   - 授权成功后，后端会写入 `Authorization`、`AccessToken`、`Token` 响应头，并重定向回回调页面。
   - 前端页面只负责跳转到后端 OAuth 授权入口和处理回跳后的登录态，不直接调用第三方平台 SDK 地址。

9. 列表展示字段也必须按排序规则展示，并按字段语义选择展示方式。
   - 数字、枚举、日期时间、名称、手机号、邮箱等后端可映射字段优先支持排序。
   - JSON、HTML、子表单等复杂字段默认不在列表直接展示。
   - 图片字段用图片预览组件。
   - 文件字段用下载动作。
   - URL 字段用链接组件。
   - 普通长文本在列表中应截断并提供 tooltip，不直接撑开表格。
   - 本条中的 hover/tooltip 约束只适用于 CRUD 界面内的 4 类区域：CRUD 列表区、新增表单、编辑表单和详情表单；不约束查询表单、表格工具栏、列设置面板、登录页、图标选择器、普通 `common-ui` 组件或非 CRUD 页面。
   - CRUD 列表区中的普通长文本 tooltip 应延迟约 1 秒显示，并限制最大宽度、最大高度、换行和内部滚动，避免鼠标扫过列表时立即弹出并遮挡行内容。新增表单、编辑表单和详情表单中的字段提示或长内容预览也应遵循同样的延迟和宽高控制。
   - CRUD 列表区、新增表单、编辑表单和详情表单中的权限、资源、接口编码、Java 类名、表达式、JSON、大量枚举项或超过 8 项的集合，不应在 hover tooltip 中全量铺开；应优先展示摘要、数量、前几项、状态标签或字段内短预览，完整内容应通过详情、弹窗、抽屉、专用查看器或点击入口查看。
10. 带 `@Options` 注解或具备选择语义的字段必须按选择型字段处理。

选择型字段基础规则：

- 组件使用下拉选择或树选择，不使用普通文本输入。
- 查询表单中，选择型字段默认应加入查询条件。
- 选择型字段默认使用单选组件。
- 当 Java 字段类型是集合对象时，选择组件必须支持多选。
- 当查询字段带 `In` / `NotIn` 查询语义时，查询表单中必须按多选处理。
- 对于枚举类型字段，如果查询 DTO 中存在带 `@In` 的 `inXxx` 字段，应优先展示 `inXxx` 多选查询字段，而不是普通 `xxx` 单选字段。
- `columnMapExpr` 用于覆盖选项值和显示字段：第 1 项为 `valueField`，第 2 项为 `labelField`，第 3 项为描述字段。
- 如果没有显式 `columnMapExpr`，默认按选项来源推导 `valueField` 和 `labelField`。
- `reloadOn` 用于声明重新加载选项的触发条件，通常只对远程 API、实体对象或 Dao 查询对象选项有效。
- 选择型字段的列表、详情和编辑回显必须先按选项匹配标签；无匹配项时保留原始值，`null`、`undefined` 和空字符串按统一空值样式展示。不得按字段名、标签或业务文案猜测其它选项。
- 整数值匹配统一将记录值和选项值转换为字符串后比较，兼容 JSON 数字与 JSON 字符串的表达差异。例如 `100` 与 `"100"` 必须匹配；该比较只影响回显和编辑选中，不改变字段按 DTO 类型声明的提交值类型。

选项来源优先级：

| 优先级 | 来源规则 | 前端处理 |
| --- | --- | --- |
| 1 | `items` 固定选项 | 直接使用本地静态 options。 |
| 2 | `dictCode` | 从系统字典接口加载；默认 `valueField=code`，`labelField=name`。 |
| 3 | `refTargetType` 为枚举类 | 通过 `EnumController` 获取枚举选项。 |
| 4 | `refTargetType` 为实体类或 Dao 查询对象 | 从目标对象列表接口加载；如果实体支持启用状态，默认附加 `enable=true`；支持搜索。 |
| 5 | `loadApi` | 作为自定义远程选项接口；如果内容是 JSON，则按完整 API 配置处理；否则按 URL 字符串处理。 |

如果同一个字段同时配置了多个来源，按上表优先级选择第一个可用来源。

字典选项规则：

- `dictCode` 对应系统字典编码。
- 前端获取数据字典必须通过 `BizDictController.retrieveByCode` 方法。
- 数据字典接口路径由字典控制器 API 模块提供，不应在页面规则中写死某个模块的 API 前缀。
- 如果 `dictCode` 是 `$@`，`$@` 不是具体字典编码，只是动态推导占位符，必须根据当前 DTO 对象上下文推导真实字典编码。
- `dictCode="$@"` 的推导规则：
  - 如果字段所属 DTO 上存在目标实体信息，则字典编码为：`目标实体完整类名.字段名`。
  - 如果当前字段是 `inXxx` 或 `notInXxx` 这类查询字段，应先去掉 `in` / `notIn` 前缀，再把剩余字段名首字母转小写后参与编码。
  - 如果无法获得目标实体信息，则字典编码为：`DTO名称.字段名`。
- 字典接口返回项默认从返回数据的 `itemList` 中取，作为前端 options。
- 字典选项默认 `valueField=code`，`labelField=name`。
- 字典选项通常一次性加载完整 options，搜索应在前端 JS 中按 `label` 匹配；可用 `value` 兜底，但不能只按 `value` 匹配。

枚举选项规则：

- 前端获取枚举必须通过 `EnumController`。
- 枚举选项应优先通过当前模块 API 层调用 `EnumController.enums` 一次性获取全部枚举，并缓存到应用内存中。
- 应用级枚举缓存只在当前网页生命周期内有效，刷新整个网页后重新加载。
- 如果全量枚举缓存中不存在目标枚举，再调用单个枚举接口，并把返回结果补写回同一个应用级缓存。
- 单个枚举类接口由 `EnumController` 提供，具体路径由对应模块 API 层维护。
- 返回数据中的 `options` 作为前端 options。
- 枚举选项的 `labelField` 默认为 `label`；`valueField` 必须以创建、更新或查询 DTO 中字段的 Java 类型决定。字符串 DTO 字段默认使用返回的 `value`（枚举 `name()`，不使用 `toString()`）；数值 DTO 字段必须使用枚举数值编码，例如 `EnumDesc.code()` 对应的 `ordinal` 或明确 `code` 字段。
- 数值 DTO 的枚举字段即使接口同时返回枚举 `name()`，也不得把名称作为选项值或提交值；页面必须配置 `valueType: 'number'`，并以数值编码完成选中、回显和提交。
- 枚举选项通常一次性加载完整 options，搜索应在前端 JS 中按 `label` 匹配；可用 `value` 兜底，但不能只按 `value` 匹配。

实体关联选项规则：

- 远程接口为目标实体的列表接口。
- 搜索参数名应优先根据目标实体控制器 `list` 接口的查询 DTO 推导。
- 推导搜索参数名时，模糊查询字段优先，例如存在 `containsName` 时优先使用 `containsName`。
- 如果目标查询 DTO 不存在可用的模糊查询字段，再退回使用 `name`。
- 如果注解显式提供 `searchParamName`，应使用该参数名作为覆盖值。
- 用户在选择框输入搜索词时，应重新调用远程 options 接口，不应只在前端过滤当前已加载 options。
- 远程搜索时最终请求参数应为：`defaultParams + { [searchParamName]: 用户输入的搜索词 }`。
- `valueField` 默认使用目标实体主键字段。
- `labelField` 按 `name`、`label`、`desc` 的顺序自动选择第一个存在字段。
- 如果目标实体是树形结构，优先使用树选择语义；默认不展示默认图标。
- 如果当前字段是集合类型，必须开启多选。
- 如果 `defaultParams` 存在，应附加到远程选项接口请求中。
- 如果目标实体支持启用状态，默认附加 `enable=true`，且搜索时不能丢失。

自定义 API 选项规则：

- `loadApi` 指向远程接口时，不应只在前端过滤当前已加载 options。
- 用户在选择框输入搜索词时，应重新调用 `loadApi`。
- 搜索词必须按 `@Options.searchParamName` 作为 URL 参数传给后端；如果 `searchParamName` 为空或未配置，优先根据接口参数推导模糊查询参数名，再退回使用 `name`。
- 远程搜索时仍必须携带 `@Options.defaultParams`。
- 如果 `@Options.reloadOn` 存在，应按其表达式监听依赖字段变化并重新加载选项。

固定选项规则：

- `items` 中每一项按 `value||label||desc` 解析。
- `value` 必须存在。
- `label` 为空时可用 `value` 兜底展示。
- `desc` 可用于 tooltip、说明或扩展展示。
- 固定选项搜索应在前端 JS 中按 `label` 匹配；可用 `value` 兜底，但不能只按 `value` 匹配。

搜索参数编码规则：

- Java 类名、字典编码、搜索词等作为 URL 参数传递时必须进行 URL 编码。
- Java 内部类名可能包含 `$`，例如 `com.xxx.User$Sex`，必须编码为 `%24` 后传递。
- 前端不应手工拼接未编码的 Java 类名到 URL 中。
