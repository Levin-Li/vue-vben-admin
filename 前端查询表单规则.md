# 前端查询表单规则

本文件是查询字段过滤、布局与交互的正式来源。共同显隐及提交资格遵循《前端表单运行时规则》；控件、选项来源遵循《前端页面开发规则》。

## 查询字段过滤

字段来自实际列表方法，开发时先过滤再按业务任务配置顺序。

   查询表单应排除或转换以下字段：
   - 排除通用控制字段：`orderBy`、`orderDir`、`selectColumns`、`pinyinName`、`containsPublicData`、`containsOrgPublicData`、`tenantShared`、`orgShared`、`optimisticLock`。
   - 排除操作日志字段（如 `actionLog`），不展示、不参与查询提交。
   - 排除密码、密钥、图片、文件、CSS、JSON、大文本、LOB、复杂对象、关联 `Fetch` 字段。
   - 组织类字段不应直接按普通输入框展示；如果是用户可查询或可编辑的组织、机构、部门字段，应按“组织树选择规则”处理。只有系统隐含、无需用户选择或由上下文自动注入的组织字段才隐藏。
   - 排除创建时间/更新时间等继承基础字段，除非已经提供明确的范围查询字段。
   - 同一业务字段的列表查询契约同时提供可用模糊参数和精确参数时，查询表单（即查询面板）必须优先展示并提交模糊参数；例如同时存在 `containsName` 与 `name`、`containsGroupName` 与 `groupName` 时，使用前者，不得同时展示或提交后者。模糊参数是否可用，必须以当前列表查询方法、查询 DTO、参数注解或已确认的接口契约为准。
   - 如果不存在可用模糊参数，查询表单才可使用精确参数。业务明确要求精确匹配时，页面静态配置或紧邻配置说明必须声明该例外及业务原因；公共 CRUD 运行时不得按字段名自动推断或替换查询参数。
   - 如果存在 `inXxx` 或 `notInXxx`，则原始 `xxx` 字段不重复展示；`in` 优先级高于 `notIn`。
   - 如果存在 `betweenXxx`，则同一字段的 `lt/gt/lte/gte/in` 类查询不重复展示。
   - 查询表单中，成对出现的“开始时间 / 结束时间”字段应合并成一个时间范围选择器，不分成两个独立输入框。
   - 查询表单中的 `textarea` 统一降级为单行输入，避免查询区高度失控。
   - 查询表单中的 `input-array` 统一降级为 `input-text`，数组值由逗号或换行输入。


## 查询布局

- 通用CRUD查询表单应采用稳定的响应式等宽列网格；字段、查询按钮、重置按钮和高级查询展开按钮都应参与同一套查询区布局，不应把按钮固定放到查询区下方独立行。
- 查询表单列数应优先按查询面板自身容器宽度计算，而不是按整个浏览器视口计算；侧边栏展开、浏览器分屏、页面局部容器变窄时，列数必须随查询面板实际可用宽度回落。
- 查询表单默认每列最小可用宽度约为 `280px`，列数最少 1 列、最多 7 列；具体列数可按 `(容器宽度 + 列间距) / (最小列宽 + 列间距)` 取整后再限制到 `1..7`。不得为了“字段少”主动降低列数，字段少只是留下空列。
- 简单模式收起时，展示查询项数量不是固定值。先计算当前查询区列数 `N`，操作区占 1 列并尽量放在所在行最后一列；以 `searchCollapsedCount` 为下限，从排序后的查询项中继续追加字段，直到操作区所在行前面的完整空列被填满，或已经展示全部查询项。普通单格字段验收口径：当 `N > 1` 时，收起态查询项数量应尽量满足 `展示数量 % N === N - 1`；如果查询项总数不足，则展示全部。这样无论操作区在第一行，还是被迫换到下一行，操作区前面的完整空列都不会空着。
- 查询面板中，普通字段不得因补位而扩展为多列宽度；补位只能通过增加展示字段或保留空列解决，跨列字段必须由页面配置显式声明。
- 在桌面宽屏等多列场景下，查询操作区应参与同一网格并在操作区内靠右对齐；如果当前行没有可用列，则自然换到下一行。这样既保持查询字段对齐，又能充分利用剩余横向空间节省高度。
- 高级查询展开后，新增查询字段仍按同一网格继续排布，操作区应移动到展开内容后的剩余列空间并保持按钮靠右；必须验证“收起”和“展开”两种状态，确保按钮不遮挡字段，查询区高度自然增长。
- 本规则适用于公共CRUD查询表单默认布局。若具体业务页面有特殊筛选器、跨列字段、顶部全局筛选条或其它明确交互需求，应通过页面配置显式声明例外，不应在公共组件里按字段名或页面路径硬编码特判。
- 查询表单列数按本节 `1..7` 规则执行，不受新增、编辑、详情、设置、弹窗、抽屉、子表单等内容型表单的 5 列上限和 `maxWidth` 规则约束；但查询字段仍必须遵守字段过滤、排序、组件降级、label 与控件左右并排等查询表单规则。
- 整数、浮点数及其它数值类型字段通常不建议作为查询条件；仅凭字段类型不得生成查询项，默认不生成。只有列表查询方法、其查询 DTO、参数注解或明确业务需求定义了数值筛选语义时，才生成对应查询项。
- 数值查询的精确匹配、范围和比较交互必须以已定义的查询语义及请求参数为准；不得仅根据实体字段名或数值类型推断区间边界、比较运算符或请求参数名称。


- 存在显式跨列字段时，按实际占用格数和换行位置补位；不得只以字段数量取模，不改变顺序、不拉宽字段、不自动改跨度。UI 设置的展开和行数配置先生效，再计算补位。

## 查询交互

- 标签和控件左右并排。默认手动查询；用户 UI 设置开启自动查询时按该明确契约执行，不被默认行为覆盖。
- 手动模式点击查询或普通输入框回车后执行，回到第一页并保留当前排序；输入法选字、下拉选项确认期间回车不得误触发。
- 重置恢复有效初始字段值及业务默认值，不立即查询，保持当前展开或收起状态；自动查询开启时，重置引起的批量赋值也不得触发请求。不实际刷新页面或重置列表排序、分页、UI 设置。
- 收起“更多”不清除已填写条件，条件继续参与查询；收起时显示“更多（已设置 N 项）”。N 按收起区域实际生效的查询项计数，不计 UI/权限排除项，不以原始参数键数代替范围控件等业务查询项数。
- 经 UI 设置或其他资格规则从有效查询表单排除的业务字段不参与请求；仅因更多收起暂不渲染的条件仍参与查询。
- 多列表查询、分页、排序和加载状态独立，共享仅来自明确配置。异步响应隔离遵循运行时规则。
