# 表单字段三段式分组与排序规范

本规范用于新增、编辑和详情表单的开发默认配置。入口规则见[前端页面开发规则](前端页面开发规则.md)，运行时展示设置、空组、单组简化和提交语义见[前端表单运行时规则](前端表单运行时规则.md)，展示设置覆盖关系见[页面开发与页面展示设置专题](页面开发与页面展示设置专题.md)。

本规范只指导开发阶段的字段排序与分组：开发者或 AI 基于实体、请求对象、后端注解和业务任务理解字段作用，再用 `layoutGroup`、`layoutGroupTitle`、`layoutOrder` 和必要的 `layoutNewRow` 明确落地。它不授权公共运行时通过字段名称、标签、类型或业务语义自动推断、排序或分组。

运行时只执行当前页面命中的 UI 展示设置。UI 展示设置已控制的标题、顺序、分组、显隐等属性以设置为准；未被设置覆盖的属性继承开发阶段静态默认值。运行时不能因为看到 `code`、`status`、`enable` 等名称就自动改变字段位置，也不能重新判断哪个业务字段更重要。

## 1. 默认结构

表单先按用户完成任务的顺序判断字段归属。通常采用以下三段结构；三段是整体信息层次，不等于每页只能创建三个分组。前置识别信息和末尾补充与维护信息通常各保持一个连续区域；中间业务信息可以按真实业务域拆成多个连续分组。

| 部分 | 推荐分组名称 | 字段范围 | 默认位置 |
| --- | --- | --- | --- |
| 前置识别信息 | 基础信息 | 名称、标题、编号、编码、类型、类别、业务状态、阶段、等级、级别，以及完成当前操作前必须确认的归属或主体字段 | 最前 |
| 中间业务信息 | 一个或多个明确业务域名称 | 当前页面真正要维护的业务字段，例如联系人、价格、规则、结算、内容、媒体、地址、配置或业务对象明细 | 中间 |
| 补充与维护信息 | 通用维护、补充与维护、状态与排序或明确维护名称 | 普通备注、普通描述、是否启用/禁用、是否可编辑、排序值、排序代码和其它管理开关；详情中的创建时间、最后修改时间等审计字段 | 最后 |

表中列举的是字段的实体语义类别，不是固定字段名，也不是运行时按字段名匹配的规则。开发阶段必须结合实体、请求对象、后端注解和用户任务理解字段作用；字段可以带实体或业务前缀，例如“客户名称”“商品编号”“租户类型”“订单状态”“会员等级”。只有在当前任务中承担对象识别、分类或阶段判断作用时，才归入前置识别信息。

“业务状态”与“是否启用”不是同一类字段。订单状态、审核状态、生命周期阶段、等级和级别帮助用户识别和理解对象，默认属于前置识别信息；是否启用、是否禁用、是否可编辑和后台维护开关默认属于末尾维护信息，不能因为都是“状态”就提前。

末尾部分统一视为通用维护字段：普通备注、普通描述、启用禁用、是否可编辑、排序值或排序码，以及创建时间、修改时间等跨实体常见维护或审计信息均放在这里。新增和编辑场景继续隐藏不应录入的审计字段；详情展示这些字段时放在最后。文章正文、合同内容、商品详情、规则正文等当前任务的核心业务内容仍属于中间业务信息，不因包含“描述”或“内容”而放入通用维护字段。

## 2. 小型表单不分组

如果当前新增、编辑或详情场景只有前置识别信息和补充与维护信息、没有中间业务信息，且实际展示字段不超过 10 个，整个表单默认不分组，直接按“前置识别信息 → 补充与维护信息”的顺序排列。

实际展示字段只统计当前场景中可见且面向用户的字段；不统计隐藏参数、仅提交字段、按钮、分组标题、其它视图字段或审计字段在新增编辑场景中的隐藏项。不得为了形式创建空组、单字段组，或把少量前置字段和维护字段拆成两个没有独立信息价值的外壳。

以下情况才考虑分组：

- 存在至少一段连续的中间业务信息；
- 实际展示字段超过 10 个，且存在能清楚命名的连续信息域；
- 有独立提交、权限、确认、复杂对象、对象集合或阅读边界等既有专项规则要求。

即使字段超过 10 个，业务关系仍然单一时也不强制拆组；分组必须帮助查找、填写或阅读。

## 3. 字段归属的判断方法

1. 先确定用户首先要识别、选择或确认什么对象，放入前置识别信息。
2. 再确定用户实际要录入、维护或阅读的业务内容，作为中间业务信息。中间部分可以按真实业务域拆成多个连续组，例如“联系人信息”“结算信息”“业务规则”。
3. 在中间业务部分中，先按用户任务、填写依赖、业务影响和使用频率判断重要性：前置条件和核心业务字段在前，依赖字段、补充字段和低频字段在后。重要性判断是开发阶段的业务分析，应落入静态页面配置。
4. 只有同一业务段内确实无法分辨重要性时，才按字段类型、控件尺寸、填写关联和视觉协调作为次级依据：相关选择器、短文本、数字或开关可相邻排布；高组件、跨列组件和长内容避免打散短字段。该兜底不能覆盖已知的业务顺序。
5. 最后放置不改变主要业务含义的维护项和附加说明。

普通“描述”“说明”“备注”默认放在末尾。文章正文、合同内容、商品详情、规则正文等如果是本次任务的核心业务内容，属于中间业务信息，不因字段名称含有“描述”或“内容”而机械后置。

创建时间、最后修改时间、操作日志等审计字段在新增和编辑表单继续按现有规则隐藏、不展示、不提交；在详情中展示时，放在最后的审计或补充区域。

## 4. 配置与展示设置要求

- 开发默认顺序必须在页面静态配置中明确；不能依赖字段声明偶然顺序、数据库字段顺序或公共组件的分类排序。字段语义理解、业务重要性判断和默认分组只属于开发阶段，不得下放给运行时。
- 分组标题使用用户可理解的中文名称。`layoutGroup` 使用稳定内部标识，不能直接把 `basic`、`status` 等英文标识展示给用户。中间业务部分存在多个业务域时，每个域使用自己的业务名称，不能全部笼统命名为“业务信息”。
- 普通静态分组通常至少包含三个字段。复杂对象、对象集合或确有独立信息意义的少字段区域需要保留标题时，页面在该组字段上显式声明 `layoutGroupForceDisplay: true`；运行时只执行这个标记，不按组名或字段类型猜测例外。
- 组间顺序和组内顺序都按用户任务决定。不同组的局部 `layoutOrder` 不能被当作全局组顺序；每个组的顺序必须有稳定且可解释的来源。
- 页面展示设置可以覆盖已支持的标题、字段顺序和分组；运行时按命中的设置直接执行，不根据字段名称追加自动排序。恢复开发默认后，未覆盖属性回到本规范定义的页面静态顺序。
- 需要前置“是否启用”或其它维护开关时，页面配置注释或对应 OpenSpec 必须说明它为何是当前用户任务的第一步；不能仅因字段类型为开关而前置。

## 5. 审查清单

- 名称、标题、编号、编码、类型、类别、业务状态、等级和级别等语义字段，是否结合实体作用位于前置识别区域，而不是按固定字段名机械匹配？
- 启用、禁用、可编辑、排序值和排序代码是否位于末尾维护区域？
- 中间业务字段是否按业务任务、字段依赖、业务影响和使用频率排序；重要或前置内容是否在前？
- 中间业务字段是否连续、可按明确业务概念命名；多个业务域是否拆成多个连续分组？
- 业务重要性无法判断时，是否才使用字段类型、控件尺寸和视觉协调作为次级排序依据？
- 只有前置识别信息和补充与维护信息、没有中间业务字段且不超过 10 个实际展示字段时，是否保持整页不分组？
- 普通描述和备注是否靠后；核心正文是否仍在中间业务区域？
- 新增、编辑、详情是否分别按实际可见字段重新判断，而非复制产生空组？
- 修改展示设置或恢复默认后，是否仍能得到稳定、符合本规范的顺序？
