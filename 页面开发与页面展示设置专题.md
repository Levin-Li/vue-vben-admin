# 页面开发与页面展示设置专题

本专题指导页面开发者和公共组件维护者处理开发默认配置与运行时 UI 展示设置的关系。重点是统一属性含义、默认值和生效规则，同时保持简单页面易于开发。

规则入口：[前端页面开发规则](前端页面开发规则.md)。提交、显隐状态、分组后处理及交互语义继续以[前端表单运行时规则](前端表单运行时规则.md)为准；查询差异见[前端查询表单规则](前端查询表单规则.md)，校验见[前端页面校验规则](前端页面校验规则.md)。本专题不替代这些专项规则。

本文的现状结论基于 2026-09-07 对公共 CRUD 源码的核对，不代表所有自定义页面或线上历史配置均已验收。后续统一原则是开发指导，不表示框架已完成统一改造；本次文档变更不修改运行时和数据。

## 1. 统一概念，保留开发便利性

页面展示配置表达页面应展示哪些内容，以及标题、顺序、分组、尺寸等展示意图。开发时提供默认值，运行时设置提供覆盖值，页面根据最终有效配置展示。

“同一配置”首先指同一套属性语义、默认值规则与生效规则，不要求以下三者具有完全相同的物理结构：

| 概念 | 用途 | 开发注意事项 |
| --- | --- | --- |
| 开发声明 | 在字段和页面附近声明默认展示意图及场景差异 | 只写必要信息，不重复抄写公共默认值 |
| 运行时设置 | 保存管理员在适用范围内设置的展示属性 | 区分明确覆盖与继承默认，不假设其等于完整页面定义 |
| 有效配置 | 在当前页面、场景和上下文中解析出的展示结果 | 设置面板回显与页面渲染应共享同一语义，不能各自猜测默认值 |

后续统一应优先收敛重复解析逻辑。允许框架把已有开发声明整理为有效配置，但不得通过扫描控制器、OpenAPI 或字段名自动拼装业务页面，也不得为了统一而引入需要开发者维护的另一份完整配置。

## 2. 当前实现与问题清单

下表把已确认的结构现状与需要验证的风险分开；“可能”不代表已经发现线上故障。

| 编号 | 现状或风险 | 影响 | 后续开发注意事项 |
| --- | --- | --- | --- |
| P01 | 已确认：开发使用 `CrudFieldConfig`，运行时使用 `CrudPageDisplayConfig`；标题、顺序和分组在两边都有表达 | 修改一边不能自然保证另一边同步 | 新增展示属性前定位开发声明、设置编辑、默认值和渲染四个入口，明确唯一语义及映射 |
| P02 | 已确认：已有共用的 `resolveCrudPageDisplayDefaults`，但字段和分组还分别在运行页面与设置面板补齐 | 缺省值处理仍有分叉，存在回显和实际展示偏离风险 | 优先复用现有公共解析；不要在设置面板复制一份页面默认规则 |
| P03 | 已确认：无已存字段顺序时运行页面使用静态布局排序，有设置时按展示配置排序；分组仍可回退静态声明 | 无设置、打开设置、保存设置是不同路径 | 验证仅打开或无意修改后保存设置，不应意外改变原有展示；发现差异按缺陷单独修复 |
| P04 | 已确认：页面配置中还有 `formMaxColumns`、`modalWidth`、`searchCollapsedCount` 等属性；展示配置另有弹窗和查询参数 | 不能宣称所有展示属性都已归口，尺寸和折叠规则需逐项追踪 | 维护属性归属与优先级；不能只增加 UI 控件而遗漏页面消费路径 |
| P05 | 已确认：展示配置还包含自动查询、快捷填写、字段提交状态、自动强制更新等行为 | 把它当作纯视觉对象移动或删除，可能改变请求和校验 | 先保持现有行为；未来拆分需独立需求、影响评估及真实请求验证 |
| P06 | 待核验风险：设置面板会补齐字段再保存，已有记录可能包含当时默认值的快照 | 开发默认值更新后，已存值可能继续覆盖；不能仅凭值相等判断用户意图 | 核对保存粒度和历史数据，区分继承、覆盖和恢复默认；不能擅自删掉“看起来等于默认”的值 |
| P07 | 待核验风险：字段新增、删除、改名或分组变化遇到已有配置 | 可能出现新字段无位置、旧引用或错误分组 | 明确新增字段默认策略与失效引用处理；字段键和组键使用稳定标识，不用显示标题关联 |
| P08 | 已确认：设置只按当前页面路由及上下文加载；当前路由缺失时不展示设置入口。另有本地表格列设置适用判断 | 相同页面在不同上下文中可能得到不同配置；来源路径、手工编码或 API 路径会导致编码与地址栏不一致；通用兜底会让无关页面共用记录 | 排查时记录当前路由、命中的设置及适用范围；不得以来源路径、页面手工编码、API 路径或固定字符串作为设置编码，也不能把局部结果误认成开发默认值失效或新增无约束本地覆盖层 |
| P09 | 待核验风险：自定义页面、字段插槽或局部 CSS 未完整接入有效配置 | 设置控件存在不等于相应属性已生效 | 为自定义区域明确支持哪些属性，核对真实渲染；不要用局部硬编码抵消已支持的 UI 设置 |

这些问题不要求在每次页面开发中重构整个框架。局部任务应复用既有契约；涉及公共语义调整时，先记录对应 OpenSpec 需求与验收范围。

## 3. 职责边界

| 内容 | 主要归属 | 边界 |
| --- | --- | --- |
| 字段标题、顺序、分组、展示状态、列宽、溢出方式及已支持的展示尺寸 | 展示声明与 UI 展示设置 | UI 对其控制的属性优先；未纳入配置契约的属性不能宣称已可配置 |
| 颜色、图标、动效、组件具体外观和交互实现 | 主题与组件代码 | 配置可以选择已支持的展示方式，具体效果由组件实现；不把任意 CSS、事件处理都配置化 |
| 页面外围留白、背景、圆角和边框 | 用户界面设置与布局实现 | 遵守页面开发规则已有归属，不能以“颜色由代码实现”为理由覆盖用户界面设置 |
| 接口字段范围、可写性、请求映射、校验和权限 | 业务及接口契约 | 展示设置不能新增接口能力、绕过权限或改变数据结构 |
| 自动查询、提交状态等当前已存在的行为配置 | 现有专项运行时契约 | 本专题不重新解释、不删除；需要拆分时单独实施 |

“隐藏”尤其不能等同于“不提交”。展示排除、视觉折叠、隐藏参数及禁用状态遵循表单运行时规则；不能通过 DOM 是否存在或 CSS 是否可见判断请求字段。

## 4. 页面开发怎么写才不增加负担

1. **先定义业务字段与组件，再补必要展示信息。** 继续在页面字段附近声明标题、默认分组和顺序；不要要求开发者先理解后端配置存储结构才能开发页面。
2. **简单页面依赖合理默认。** 不要求为查询、新增、编辑、详情重复填写整套相同属性；确有场景差异时明确声明差异，不依赖公共组件猜测业务。
3. **每个展示属性只维护一个开发默认来源。** 不在字段配置、模板、设置初始化和 CSS 中分别写互相竞争的默认值。现有重复入口需要调整时，先核对已有优先级。
4. **复杂交互使用代码。** 联动请求、业务操作、自定义编辑组件继续用明确代码和既有扩展入口实现，不为“全部配置化”建立表达式语言或通用页面生成器。
5. **新增展示能力同时考虑设置回显。** 明确类型、默认值、适用场景、覆盖语义、编辑控件和页面消费方式。只提供配置项而没有执行路径，不算完成。
6. **现有 API 未支持的能力先记录需求。** 不照着本文自创同名属性并假设框架会自动识别；更改公共配置结构需先补 OpenSpec。

例如，字段开发声明使用标题“姓名”、分组“基本信息”：未配置覆盖时，页面和设置面板都应表达这一默认意图；管理员只修改标题时，分组应继续按其既定继承或覆盖契约处理。具体存储如何表达“只修改标题”，需要核对现有实现，本文不声明已经支持属性级差量保存。

## 5. 默认、覆盖与恢复默认的注意事项

后续维护或统一实现时，必须先定义以下状态，不能依靠真假值或空值一概处理：

| 状态 | 应明确的语义 |
| --- | --- |
| 未设置 | 继承适用的开发默认及公共默认 |
| 明确设置 | 使用该属性允许的覆盖值，包括合法的 `false`、`0` 等 |
| 明确清空 | 根据属性契约表示空标题、无分组等；不自动等同于继承 |
| 恢复默认 | 恢复指定属性、视图或页面的继承关系；范围必须明确 |

- 对每个属性约定 `undefined`、`null`、空字符串和空数组含义，不直接使用统一深合并或 `value || default` 代替业务语义。
- 字段和组列表需要按稳定键处理，不能按数组下标关联；顺序也不能仅靠对象合并自然得出。
- 缺省值与用户覆盖值应在解析过程中可区分，便于解释当前结果来自哪里；不要求普通页面开发者额外维护整套来源数据。
- 保存设置时，不应无意把未修改的继承属性全部固化。若当前存储采用完整快照，必须先确认语义和迁移方案，再修改保存行为。
- “恢复默认”不应只是复制当前默认值并继续当作用户覆盖，否则后续默认值变化仍无法继承。现有恢复能力需按其实际范围核验，不能据本文推定全部已实现。
- 配置读取、响应式重排、空组隐藏和单组简化不得自动改写持久化配置。保存动作与临时展示状态分别处理。

## 6. 生效顺序与演进边界

展示属性的方向性原则是：公共默认提供缺省基准，开发声明补充业务展示意图，命中的运行时设置覆盖其管理的属性。接口范围、权限和场景约束始终有效；有效字段、提交范围、空组及响应式处理的具体顺序以表单运行时规则为准。

设置面板与页面应复用公共解析出的展示语义，但设置面板仍需保留可编辑的候选字段，不能因当前页面隐藏了字段而让管理员无法重新开启它。设置草稿也不能直接修改已生效配置；保存、取消和失败后的状态按既有规则处理。

后续统一建议按属性逐步收敛：先确认属性对应关系和回归证据，再统一默认解析与消费路径，最后清理重复入口。不要同时改字段键、持久化结构和提交语义，使问题难以归因。

涉及历史配置时，遵守仓库迁移治理：先评估旧记录、失效引用及恢复策略，通过独立显式迁移处理，不在加载设置时自动转换或回写；运行时兼容分支须按根规则取得明确确认。仅发布新前端版本不等于获得历史数据迁移授权。

## 7. 后续开发与审查检查表

以下是未来受影响功能的验收要点，不表示本次文档变更已执行这些测试：

- 无服务端设置：页面实际展示与开发默认、设置面板回显一致。
- 只打开并关闭设置、无改动保存：不意外改变字段顺序、分组、尺寸或请求行为。
- 只覆盖一个属性：其它属性遵守已定义的继承规则；刷新和重新进入后结果一致。
- 明确隐藏、清空、`false`、合法 `0` 和恢复默认：不被兜底逻辑覆盖；恢复范围明确。
- 更新开发默认：无覆盖字段跟随变化，有覆盖字段保持用户意图；已有快照按实际契约验证。
- 字段新增、删除、改名和组变化：没有错配、无效引用或静默持久化迁移。
- 查询、新增、编辑、详情、列表分别验证；多列表和自定义字段核对各自接入范围。
- 切换适用上下文、重新加载及保存失败：不串用设置，不污染生效值或丢失草稿。
- UI 变化后复核真实请求：权限、校验、有效提交范围、必要隐藏参数和强制更新字段不被意外改变。
- 普通与窄屏、空组与多组验证：展示后处理不恢复已排除内容、不改写用户设置。

公共解析变更用针对性单测保护上述属性语义；影响实际页面时还需按前端模块规则完成浏览器验证。不能用类型检查通过代替设置确实生效的验证。

## 8. 当前源码定位

以下入口用于核对现状，符号名比固定行号更适合持续查找：

- [配置类型](packages/business/admin-framework/src/framework-commons/shared/types.ts)：`CrudFieldConfig`、`CrudPageDisplayConfig` 及页面配置中的尺寸、折叠属性。
- [共用展示工具](packages/business/admin-framework/src/framework-commons/shared/crud-page-display.ts)：`resolveCrudPageDisplayDefaults`、`resolveStaticDisplayGroup`、`canUseLocalTableColumnSettings`。
- [页面运行时](packages/business/admin-framework/src/framework-commons/shared/crud-page.vue)：`loadPageDisplaySettings`、`savePageDisplaySettings`、`getPageDisplayField`、`hasStoredFieldOrder`。
- [设置面板](packages/business/admin-framework/src/framework-commons/shared/page-display-settings-drawer.vue)：`ensureFields`、`getDevelopmentDefaultGroups` 及草稿保存逻辑。
- [设置加载服务](packages/business/admin-framework/src/framework-commons/app/api/ui-setting-runtime.ts)：设置解析及缓存入口。

修改上述契约后，同步维护本专题中的现状描述；不得把历史观察长期当作当前实现事实。
